package com.gildedrose.service.quality;

import com.gildedrose.Item;

public class LegendaryQualityService implements QualityService {
    /**
     * Updates the quality of a legendary item.
     * Because this item does not change in quality
     * the method is empty
     *
     * @param item
     */
    @Override
    public void update(Item item) {
    }
}
